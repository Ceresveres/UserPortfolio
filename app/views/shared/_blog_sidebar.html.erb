<div class="col-sm-3">
	<div class="p-3 mb-3 bg-light rounded">
	  <h4 class="font-italic">About</h4>
	  <p class="mb-0">This is a new site, so the blog posts will be a bit sparse for the time being. I plan on actively maintaining and updating this blog site.</p>
	</div>
	<div class="p-3 mb-3 bg-light rounded">
  	<h4 class="font-italic topic"><%= link_to "Topics",topics_path %></h4>
  	<% @side_bar_topics.each do |topic| %>
  		<p><%= link_to topic.title, topic_path(topic) %></p>
  	<% end %>
	</div>
</div>